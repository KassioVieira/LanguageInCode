import React from 'react';
import { AppName, WrapperTop } from './TopBar.styles';

const TopBar = () => {
  return (
    <WrapperTop>
      <AppName>Language In Code</AppName>
    </WrapperTop>
  );
};

export default TopBar;
